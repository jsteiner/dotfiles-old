#!/bin/sh

battery_percentage="$(pmset -g batt | sed -E '/.*([0-9]{2,3})%.*/!d; s//\1/;')"

if [ $battery_percentage -lt 20 ]; then
  printf "#[fg=#990000]%d%%#[fg=black]" "$battery_percentage"
else
  printf "%d%%" "$battery_percentage"
fi
